name: "image-resize"
description: "Resize image to specific dimensions"
steps:
  - operation: "resize"
    input: "${input}"
    output: "${output}/resized.jpg"
    params:
      width: 800
      height: 600
      quality: 85
